**Hazırlayan:** Aydın Alizade
**Kaynak Eğitimler:**

* [Python Mucizesi - Temelden Uzmanlığa](https://www.udemy.com/share/108O9A3@Ois2RVfVXhnK9_UwpeOKdzOJB3ofPW_KMN5kwAE20e0uxPFIi8kHtbWD_8Wj9PxA/)

---

## 1️⃣ Data Types (Veri Tipleri)

Python'da veri tipleri, değişkenlerde tutulan verinin türünü belirler.
Temel veri tipleri şunlardır:

| Veri Tipi | Örnek                      | Açıklama                              |
| --------- | -------------------------- | ------------------------------------- |
| `int`     | 10                         | Tam sayılar                           |
| `float`   | 3.14                       | Ondalıklı sayılar                     |
| `str`     | "Aydın"                    | Metin (string) ifadeler               |
| `bool`    | True / False               | Mantıksal değerler                    |
| `list`    | [1, 2, 3]                  | Sıralı, değiştirilebilir koleksiyon   |
| `tuple`   | (1, 2, 3)                  | Sıralı, **değiştirilemez** koleksiyon |
| `dict`    | {"ad": "Aydın", "yaş": 21} | Anahtar-değer çiftlerinden oluşur     |

**Örnek:**

```python
ad = "Aydın"
yas = 21
ogrenciler = ["Ali", "Mehmet", "Aydın"]
print(type(yas))  # <class 'int'>
```

---

## 2️⃣ Conditionals (Koşullar – if, elif, else)

Koşullar, belirli durumlarda farklı işlemler yapılmasını sağlar.

**Örnek:**

```python
puan = 85

if puan >= 90:
    print("Mükemmel!")
elif puan >= 70:
    print("Başarılı!")
else:
    print("Daha fazla çalışmalısın.")
```

---

## 3️⃣ Loops (Döngüler – for, while)

### 🔹 `for` döngüsü

Liste veya aralıklar üzerinde döner.

```python
for i in range(5):
    print("Sayım:", i)
```

### 🔹 `while` döngüsü

Koşul sağlandığı sürece devam eder.

```python
sayac = 0
while sayac < 3:
    print("Tekrar:", sayac)
    sayac += 1
```

---

## 4️⃣ Functions (Fonksiyonlar)

Fonksiyonlar belirli görevleri gerçekleştiren kod bloklarıdır.
Kodun tekrarlanmasını önler ve düzenli hale getirir.

```python
def topla(a, b):
    return a + b

sonuc = topla(3, 5)
print("Toplam:", sonuc)
```

---

## 5️⃣ Class (Sınıflar)

Sınıflar, nesne yönelimli programlamanın temelidir.
Bir sınıf (class), hem verileri (özellikleri) hem de bu verilerle ilgili işlemleri (metodları) bir arada tutar.

```python
class Hesap:
    def __init__(self, sahip_adi, bakiye=0):
        # __init__ metodu, sınıf çağrıldığında otomatik olarak çalışır.
        # self.sahip_adi = sahip_adi ifadesiyle, girilen isim o nesneye atanır.
        # bakiye için başlangıç değeri verilmezse 0 alınır.
        self.sahip_adi = sahip_adi
        self.bakiye = bakiye

    def para_yatir(self, miktar):
        # Bu metod, self.bakiye değerini artırır.
        self.bakiye += miktar
        print(f"{miktar} TL yatırıldı. Yeni bakiye: {self.bakiye}")

    def para_cek(self, miktar):
        # Çekilmek istenen miktar bakiyeden büyükse hata verir.
        if miktar > self.bakiye:
            print("Yetersiz bakiye!")
        else:
            self.bakiye -= miktar
            print(f"{miktar} TL çekildi. Kalan bakiye: {self.bakiye}")

    def bakiye_goster(self):
        print(f"Güncel bakiye: {self.bakiye}")

hesap1 = Hesap("Aydın")
hesap1.para_yatir(500)
hesap1.para_cek(200)
hesap1.bakiye_goster()
```

### 🔹 Öğrendiğim ek detaylar

* `__init__` metodu, **otomatik çalışan bir kurucu** (constructor) metottur.
* Eğer sınıf çağrılırken `bakiye` değeri girilmezse, **varsayılan olarak 0** kabul edilir.
* Normal fonksiyonlarda self yazılmaz çünkü fonksiyon bir sınıfın içinde değil, bağımsızdır.
* `self` olmadan sınıf içindeki değişkenlere erişim mümkün değildir.
* Class içindeki her metodun ilk parametresi self olmalıdır. Çünkü Python, sınıf içindeki metodları çağırırken otomatik olarak o nesneyi (objeyi) ilk argüman olarak gönderir.
* Bu yapı sayesinde birden fazla hesap nesnesi oluşturabiliriz ve her biri kendi verilerini saklar.

---

**Not:** Bu dokümantasyon, temel Python konularının kısa bir özeti olup, Udemy derslerinden alınan bilgiler ile kendi öğrendiğim notlarımdan derlenmiştir.